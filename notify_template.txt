Build={{build}}, Flavor = {{flavor}}, Group = {{group}}
======================================================================================================================
+ID      || Name     || Instance Type   ||  Result   || HDD           || Failed Modules     ||  BugRefs               +
======================================================================================================================
{% for item in items %}
+{{item.id}}||{{item.name}}||{{item.instance_type}}||{{item.result}}||{{item.hdd}}||{{item.failed_modules}}||{{item.bugrefs}}+
{% endfor %}
=======================================================================================================================
